#### Jsp  프로젝트 에픽게임즈

 ## 오라클 12C 사용자 생성
```sql
alter session set "_ORACLE_SCRIPT"=true;  
CREATE USER cos IDENTIFIED BY bitc5600;
GRANT CREATE SESSION TO cos;
GRANT CREATE TABLESPACE TO cos;
GRANT CREATE TABLE TO cos;
GRANT CREATE SEQUENCE TO cos;
GRANT select, insert, delete, update ON cos.player TO cos;
alter user cos default tablespace users quota unlimited on users;
```

## 테이블
```sql
CREATE TABLE "GAMEINFO" 
   (	"ID" NUMBER NOT NULL ENABLE primary key, 
	"GAMENAME" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
	"PRICE" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
	"IMG" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"MOVIE" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"DEVELOPER" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"PUBLISHER" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"EXPLANATION" VARCHAR2(4000 BYTE) NOT NULL ENABLE, 
	"TAGS" VARCHAR2(1000 BYTE) NOT NULL ENABLE, 
	"RATING" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"RELEASEDATE" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"PLATFORM" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"MINOS" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"MINPROCESSOR" VARCHAR2(1000 BYTE) NOT NULL ENABLE, 
	"MINMEMORY" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"MINGRAPHICS" VARCHAR2(1000 BYTE) NOT NULL ENABLE, 
	"MINDIRECTX" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"MINSTORAGE" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"RECOMOS" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"RECOMPROCESSOR" VARCHAR2(1000 BYTE) NOT NULL ENABLE, 
	"RECOMMEMORY" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"RECOMGRAPHICS" VARCHAR2(1000 BYTE) NOT NULL ENABLE, 
	"RECOMDIRECTX" VARCHAR2(2000 BYTE) NOT NULL ENABLE, 
	"RECOMSTORAGE" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"LANGUAGESSUP" VARCHAR2(1000 BYTE) NOT NULL ENABLE, 
	"FREEDOWN" VARCHAR2(200 BYTE), 
	"GAMEPLAYIMAGE1" VARCHAR2(200 BYTE), 
	"GAMEPLAYIMAGE2" VARCHAR2(200 BYTE), 
	"GAMEPLAYIMAGE3" VARCHAR2(200 BYTE), 
	"CONTENTIMAGE" VARCHAR2(200 BYTE), 
	"CONTENT" VARCHAR2(4000 BYTE), 
	"BUYCOUNT" NUMBER 
	CONSTRAINT "GAMEINFO_PK" PRIMARY KEY ("ID")
);

CREATE TABLE "USERS" 
   (	"ID" NUMBER, 
	"USERNAME" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
	"DISPLAYNAME" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
	"USERPROFILE" VARCHAR2(200 BYTE), 
	"USERROLE" VARCHAR2(20 BYTE), 
	"CREATEDATE" TIMESTAMP (6), 
	"TEL" VARCHAR2(200 BYTE), 
	"ADDRESS" VARCHAR2(2000 BYTE), 
	"COMPANY" VARCHAR2(2000 BYTE), 
	"COMPANYVAT" VARCHAR2(220 BYTE), 
	"COMPANYADDRESS" VARCHAR2(2000 BYTE), 
	 PRIMARY KEY ("ID")
);

CREATE TABLE "ANSWER" 
   (	"ANSWERID" NUMBER NOT NULL ENABLE, 
	"USERID" NUMBER, 
	"DISPLAYNAME" VARCHAR2(200 BYTE), 
	"EMAIL" VARCHAR2(200 BYTE), 
	"PLATFORM" VARCHAR2(200 BYTE), 
	"LANGUAGE" VARCHAR2(200 BYTE), 
	"QUESTION" VARCHAR2(4000 BYTE), 
	"ANSWER" VARCHAR2(4000 BYTE), 
	"WRITEDATE" VARCHAR2(200 BYTE), 
	"ANSWERDATE" TIMESTAMP (6), 
	 CONSTRAINT "ANSWER_PK" PRIMARY KEY ("ANSWERID")
);

CREATE TABLE "CART" 
   (	"ID" NUMBER NOT NULL ENABLE, 
	"GAMEID" NUMBER NOT NULL ENABLE, 
	"USERID" NUMBER NOT NULL ENABLE, 
	"GAMENAME" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"CONTENTIMAGE" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"PRICE" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	 CONSTRAINT "CART_PK" PRIMARY KEY ("ID")
);

CREATE TABLE "GAMECODE" 
   (	"CODEID" NUMBER NOT NULL ENABLE, 
	"GAMEID" NUMBER NOT NULL ENABLE, 
	"GAMENAME" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"CODE" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	 CONSTRAINT "GAMECODE_PK" PRIMARY KEY ("CODEID")

);

CREATE TABLE "GAMENEWS" 
   (	"ID" NUMBER NOT NULL ENABLE, 
	"GAMEID" NUMBER NOT NULL ENABLE, 
	"IMAGE" VARCHAR2(1000 BYTE) NOT NULL ENABLE, 
	"CONTENT" VARCHAR2(1000 BYTE) NOT NULL ENABLE, 
	"CREATEDATE" TIMESTAMP (6) NOT NULL ENABLE, 
	"TIMEAGO" VARCHAR2(20 BYTE), 
	 CONSTRAINT "GAMENEWS_PK" PRIMARY KEY ("ID")
);

CREATE TABLE "USEDGAMECODE" 
   (	"ID" NUMBER NOT NULL ENABLE, 
	"USERID" NUMBER NOT NULL ENABLE, 
	"GAMEID" NUMBER NOT NULL ENABLE, 
	"GAMENAME" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"GAMECODE" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"USEDATE" TIMESTAMP (6) NOT NULL ENABLE, 
	 CONSTRAINT "USEDGAMECODE_PK" PRIMARY KEY ("ID")
);

CREATE TABLE "USERORDERLIST" 
   (	"ORDERID" NUMBER NOT NULL ENABLE, 
	"GAMEID" NUMBER NOT NULL ENABLE, 
	"USERID" NUMBER NOT NULL ENABLE, 
	"GAMENAME" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"BUYDATE" TIMESTAMP (6) NOT NULL ENABLE, 
	 CONSTRAINT "USERORDERLIST_PK" PRIMARY KEY ("ORDERID")
);

CREATE TABLE "USERQUESTION" 
   (	"QUESTIONID" NUMBER NOT NULL ENABLE, 
	"USERID" NUMBER NOT NULL ENABLE, 
	"DISPLAYNAME" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"PLATFORM" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"LANGUAGE" VARCHAR2(200 BYTE) NOT NULL ENABLE, 
	"QUESTION" VARCHAR2(4000 BYTE) NOT NULL ENABLE, 
	"WRITEDATE" TIMESTAMP (6) NOT NULL ENABLE, 
	 CONSTRAINT "USERQUESTION_PK" PRIMARY KEY ("QUESTIONID")
);
```

##시퀀스
```sql
CREATE SEQUENCE GAMEINFO_SEQ
    START WITH 1
    INCREMENT BY 1;
    
CREATE SEQUENCE USERS_SEQ
    START WITH 1
    INCREMENT BY 1;
    
 CREATE SEQUENCE ANSWER_SEQ
    START WITH 1
    INCREMENT BY 1;

 CREATE SEQUENCE CART_SEQ
    START WITH 1
    INCREMENT BY 1;

 CREATE SEQUENCE GAMECODE_SEQ
    START WITH 1
    INCREMENT BY 1;

 CREATE SEQUENCE GAMENEWS_SEQ
    START WITH 1
    INCREMENT BY 1;

 CREATE SEQUENCE QUESTION_SEQ
    START WITH 1
    INCREMENT BY 1;

 CREATE SEQUENCE USEDCODE_SEQ
    START WITH 1
    INCREMENT BY 1;

 CREATE SEQUENCE BUYLIST_SEQ
    START WITH 1
    INCREMENT BY 1;
```
